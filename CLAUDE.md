# ML Auto Flow / JMDC — 프로젝트 지침

## 절대 불변식
1. **Python 재현성(★최우선):** **모든 데이터 분석은 오직 검증 가능한 Python으로 수행된다.** 캔버스 모듈은 브라우저 Pyodide로 실행되고, "전체 코드 보기"는 외부에서 그대로 도는 standalone Python을 내보내며, `npm run verify:pipelines`가 외부 Python **2회 실행 byte-identical**로 이를 강제한다. `data_analysis_modules.py` ↔ `codeSnippets.ts` 정합성을 항상 유지한다.
   - **새 분석 모듈 추가 시 반드시 ① `codeSnippets.ts` export 템플릿(결정적, 시드 고정) + ② `verify/pipelines/` 픽스처를 함께 추가**한다. "설정만 출력"하는 인앱 전용 스텁은 금지(분석 로직이 export에 빠지면 외부 재현 불가 = 불변식 위반).
   - 2026-06-23 전수 점검·복구 완료: RandomForest·LogisticRegression/SVM/LDA/NaiveBayes(분류 4종)·Correlation/OutlierDetector/HypothesisTesting(통계 3종) export 갭 해소, FeatureEngineer·FeatureImportance·PythonScript 신설. ML verify 27/27. (웹 Pyodide 한계로 인앱 미지원인 최신 기법은 `docs/azure_ml_book/05` 참조 — 내보낸 코드는 사용자 환경에서 무제한 확장 가능.)
   - **✅ 해결됨(2026-06-23): 인앱 RandomForest/GradientBoosting 정확도.** 진단 정정: RF/GB는 인앱 TrainModel 전용 분기가 없어 `Math.random()` 시뮬레이션 폴백(가짜·비결정적 계수·메트릭)에 빠졌고 ScoreModel은 그 난수에 선형식을 적용했다(DecisionTree/KNN/NeuralNetwork는 이미 실제 적합+재적합 스코어링으로 정상이었음 — 이전 "선형 근사" 기술은 부정확했음). 수정: `fit/scoreRandomForestPython`·`fit/scoreGradientBoostingPython` 신설(sklearn.ensemble, random_state=42 결정적) + TrainModel 회귀·분류 분기 + ScoreModel 재적합 디스패치. 실제 브라우저 Pyodide 검증(RF R²≈0.99·GB≈0.999·feature importance 실제 관계 복원·재현 동일), export·verify 불변. 3개 앱 공통 적용. 상세: `docs/azure_ml_book/05` §8.
   - **✅ 해결됨(2026-06-23): 인앱 SVM/LDA/NaiveBayes ScoreModel 크래시.** App.tsx ScoreModel이 호출하나 `pyodideRunner.ts`에 정의가 없던 `scoreSVMPython`·`scoreLDAPython`·`scoreNaiveBayesPython`(+DFA는 `scoreDecisionTreePython`)를 신설(훈련 데이터 재적합 후 `.predict`, fit* 정합; SVM은 probability=True·random_state=42). 인앱 전용·가산적, export·verify 무관. 브라우저 Pyodide 검증(분류 정확도 1.0·proba 출력). ML↔JMDC byte-identical, DFA 동일 함수 본문. 상세: `docs/azure_ml_book/05` §8.
2. **두 앱 동기화:** `ML Auto Flow`(베이스)와 `ML_Auto_Flow-JMDC`(JMDC 상위집합)의 공통 변경은 양쪽에 동일 적용하고, JMDC 전용만 차이로 남긴다.

## 하네스: ML Flow (두 앱 수정·강화·테스트)

**목표:** 두 앱의 Python 모듈 강화·AI 기능·프론트엔드 변경을 에이전트 팀으로 조율하되 위 두 불변식을 지킨다.

**트리거:** 두 앱 관련 수정·강화·모듈/AI/성능 작업 또는 "다시 실행/재실행/업데이트/이어서/다른 앱에도/보완" 요청 시 `ml-flow-orchestrator` 스킬을 사용하라. 단순 사실 질문은 직접 응답 가능.

**구성:** 에이전트 `.claude/agents/`(python-module-engineer, ai-feature-engineer, frontend-engineer, dual-app-sync, qa-verifier), 스킬 `.claude/skills/`(ml-flow-orchestrator, python-reproducibility, dual-app-sync, ai-key-and-features).

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-06-07 | 초기 구성 | 전체 | 하네스 신규 구축 |
| 2026-06-07 | AI 로컬 API 키화 (첫 실행) | lib/aiClient.ts, ApiKeySettingsModal.tsx, App.tsx, PropertiesPanel.tsx, 8개 모달, vite.config.ts (양쪽 앱) | 번들 하드코딩 키 제거→사용자별 localStorage 키 입력+중앙 클라이언트 통일, dev env 폴백 유지 |
| 2026-06-07 | AI 기능 확장 #1 | lib/aiHelpers.ts, PipelineCodeModal.tsx (양쪽 앱) | 재사용 AI 헬퍼(코드해설/결과해설/오류수정) + 내보낸 Python 코드 'AI 설명' 통합. Python 재현성 강화 |
| 2026-06-07 | AI 기능 확장 #2 | StatisticsPreviewModal/EvaluationPreviewModal/EvaluateStatPreviewModal.tsx (양쪽 앱) | 결과 미리보기에 explainModuleResult ✨ 해설 통합 (frontend-engineer 위임) |
| 2026-06-07 | AI 기능 확장 #3 | ErrorModal.tsx (양쪽 앱) | 모듈 오류에 suggestErrorFix ✨ 원인분석 통합 (ai-feature-engineer 위임) |
| 2026-06-07 | Python 재현성 강화 | data_analysis_modules.py, codeSnippets.ts (양쪽 앱) | split 내보내기 코드 random_state None→42 폴백(자기재현 보장), create_neural_network 누락 random_state=42 추가(NameError+결정성). 앱 런타임 출력 불변 |
| 2026-06-07 | 전체코드 standalone 실행성+재현성 수정 | codeSnippets.ts, utils/generatePipelineCode.ts (양쪽 앱) | 전체 파이프라인 코드가 외부 Python에서 바로 실행되고 동일 결과 재현되도록 5개 버그 수정: ①객체파라미터 JS불리언→Python리터럴(toPyLiteral) ②주석된 실행호출 활성화 ③빈 컬럼선택→전체수치형 ④모델생성 모듈 변수매핑 ⑤SplitData train/test 포트 구분. 실제 Python 3회 실행 byte-identical 검증 |
| 2026-06-07 | 전체코드 실행위주 간결화 | utils/generatePipelineCode.ts (양쪽 앱) | 전체코드는 설명주석/배너 제거+1줄 헤더로 실행 위주 표시(stripCommentsKeepCode). 개별 모듈 코드(PropertiesPanel getModuleCode)는 옵션 그대로 유지 |
| 2026-06-07 | AI 기능 확장 #4 | CorrelationPreviewModal/HypothesisTestingPreviewModal/NormalityCheckerPreviewModal/OutlierDetectorPreviewModal.tsx (양쪽 앱) | 4개 통계 미리보기 모달에 explainModuleResult ✨ 결과 해설 통합(기존 패턴 동일). 양쪽 앱 byte-identical |
| 2026-06-07 | 회귀 검증 하네스 추가 | verify/ (run-verification.mjs, generate.ts, pipelines/*.json, README), package.json(verify:pipelines), .gitignore (양쪽 앱) | 전체 파이프라인 코드가 외부 python으로 바로 실행+2회 byte-identical 재현되는지 자동 회귀 검증. 픽스처 5개 5/5 PASS(양쪽). 클러스터링 모듈은 Python 템플릿 부재로 범위 외(README 명시) |
| 2026-06-07 | 클러스터링 전체코드 템플릿 추가 | codeSnippets.ts(KMeans/PCA/TrainClusteringModel/ClusteringData), generatePipelineCode.ts(MODULE_OUTPUT_VAR), verify/pipelines/07_clustering_kmeans.json, verify/README (양쪽 앱) | 클러스터링 K-Means 체인을 전체코드로 내보내 외부 python 실행+재현 가능하게 함(지도학습 TrainModel/ScoreModel 변수 와이어링 미러, 시드 42 고정). verify 6/6 PASS(양쪽). PCA 템플릿도 추가했으나 DEFAULT_MODULES의 PCA가 PrincipalComponentAnalysis(미존재 ModuleType) 오타라 fixture 보류 |
| 2026-06-07 | PCA ModuleType 정합성 수정 | types.ts(PrincipalComponentAnalysis="PCA" 별칭), verify/pipelines/08_pca.json, verify/README (양쪽 앱) | enum에 없던 PrincipalComponentAnalysis(런타임 undefined, App.tsx/ComponentRenderer/PropertiesPanel/constants 11곳 참조)를 동일 값 별칭으로 추가→죽어있던 PCA 분기가 실제 PCA 모듈과 매칭. PCA 체인 fixture(08) 추가 검증. verify 7/7 PASS, build 성공(양쪽) |
| 2026-06-07 | DBSCAN 클러스터링 모듈 신설 | types.ts(DBSCAN enum), constants.ts(팔레트+DEFAULT_MODULES), codeSnippets.ts(DBSCAN 템플릿+ClusteringData labels_ 분기), components/PropertiesPanel.tsx(eps·min_samples 편집기), components/ComponentRenderer.tsx(noRunButtonTypes), verify/pipelines/09_dbscan.json, verify/README (양쪽 앱) | 밀도 기반 DBSCAN을 정식 모듈로 추가. transductive(.predict 없음)라 ClusteringData가 .labels_ 분기로 클러스터(-1=노이즈) 할당. eps=0.5/min_samples=5 기본, 결정적이라 시드 불필요. verify 8/8 PASS, build 성공(양쪽). 계층적(Agglomerative)은 범위 외 |
| 2026-06-07 | 계층적 클러스터링 모듈 신설 | types.ts(HierarchicalClustering enum), constants.ts(팔레트+DEFAULT_MODULES), codeSnippets.ts(AgglomerativeClustering 템플릿), components/PropertiesPanel.tsx(n_clusters·linkage 편집기), components/ComponentRenderer.tsx, verify/pipelines/10_hierarchical.json, verify/README (양쪽 앱) | AgglomerativeClustering을 정식 모듈로 추가(DBSCAN과 동일 transductive→ClusteringData .labels_ 분기 재사용). sklearn 1.7 호환 위해 affinity→metric, ward는 euclidean 강제. verify 9/9 PASS, build 성공(양쪽). 클러스터링 패밀리(KMeans·PCA·DBSCAN·Hierarchical) 전체코드 내보내기 완성 |
| 2026-06-13 | 고급기능 비밀번호 게이트 신설 | contexts/AdvancedFeatureContext.tsx(신규: Provider+useAdvancedFeature+AdvancedOnly+ADVANCED_BTN_DIM+AdvancedLockBadge, SHA-256 검증, localStorage 유지), components/AdvancedUnlockModal.tsx(신규), index.tsx(Provider 마운트), App.tsx(상단 우측 '고급기능 실행' 버튼+잠금상태, AI 2종/PPT/전체코드/⚙API설정 게이팅), components/PropertiesPanel.tsx(코드탭+AI 추천/파라미터설명/데이터사전분석 게이팅), 미리보기 15모달(✨AI 해설·결과해석·원인분석·AI설명 버튼 게이팅) (양쪽 앱) | AI 파이프라인 생성·AI 데이터 분석·PPT·코드보기/내보내기·API키 등 API·코드 관련 고급기능을 비밀번호로 잠금. 일반 사용자는 모듈 배치·연결·실행·결과 미리보기만 가능. 해제 시 고급 버튼은 🔒배지+흐림(opacity-60)으로 구분. ADVANCED_PASSWORD_HASH는 빈 값 기본(미설정 시 항상 잠금)—사용자 지정 비밀번호의 SHA-256으로 채워야 활성화. Playwright로 잠금/해제 흐름·핵심기능(샘플 로드·캔버스) 검증, build 성공(양쪽) |
| 2026-06-22 | 횡단 공통 입출력(I/O) 개선 6종 적용 | (P1)components/SamplesModal.tsx+samples/samples-metadata.json(메타 스키마 tags/dataFile/expectedOutput/modelOrTask), (P2)samples/Book_*.json+verify/pipelines/11~13+verify/datasets/*(JMDC 이식)+samples/JMDC_Cohort_IncidenceSurvival.json(JMDC전용), (P3)utils/dataOverview.ts+components/DataOverviewPanel.tsx+DataPreviewModal.tsx(데이터 개요 패널), (P4)codeSnippets.ts+components/PropertiesPanel.tsx+server/split-data-server.js+vite.config.ts(URL 로더+/api/proxy-csv 프록시, sourceType=url 가산 분기), (P5)utils/scoringExport.ts+components/PipelineCodePanel.tsx(스코어링 코드 내보내기, 고급게이트), (P6)verify 재확인 (양쪽 앱) | docs/cross_app_io_improvements.md 기반 6단계 공통 I/O 개선을 4개 앱에 단계적 적용. 모두 가산적·하위호환, LoadData/실행/연결/시각화 불변식 불훼손. URL은 입력층 fetch+기존 파서로 진입(Pyodide 실행 무변경)→verify:pipelines 12/12 PASS 유지(양쪽), build 성공(양쪽). 공통 코드 byte-identical, JMDC 전용=cohort 샘플 1종. DFA/life matrix는 각 앱 docs/book_based_plan 기준 동등 적용(별도) |
| 2026-06-22 | EvaluateModel ROC/AUC 강화(2-1)+회귀지표 정합(2-2) | codeSnippets.ts(EvaluateModel 템플릿: 분류 ROC-AUC·Average Precision 추가, trained_model.predict_proba/decision_function에서 점수 재도출, 이진=positive-class·다중=OVR macro·try/except 가드·확률 없으면 N/A; 회귀 RSE·RAE 추가), data_analysis_modules.py(evaluate_model 동일 가산: ROC-AUC/PR-AUC·MAE·RSE·RAE, feature_columns 인자 추가), utils/pyodideRunner.ts(앱 내 평가: 이진 확률 시 AUC-ROC·Average Precision, 회귀 RSE·RAE), components/EvaluationPreviewModal.tsx(임계값 슬라이더 신설=미리계산 threshold표에서 선택·앱전용·내보낸코드 무관, PR 곡선 차트 신설, ROC 차트는 기존 유지) (양쪽 앱) | docs/azure_ml_book/01_book_based_improvements.md 2-1·2-2 구현. 전부 가산적(기존 accuracy/precision/recall/F1·혼동행렬·MSE/R² 보존). 신규 계산은 결정적(고정 예측·고정 모델→AUC/곡선/상대오차 결정적), 출력은 .6f 고정·딕셔너리 순서 안정. 슬라이더는 in-app 전용이라 재현성 무영향. verify:pipelines 13/13 PASS 유지(양쪽, 이진 adult·다중 iris·회귀 boston/automobile 포함), build 성공(양쪽). 변경 코드 4파일 모두 양쪽 byte-identical |
| 2026-06-23 | Elston 책 기반 개선 3종 + 자전거 수요예측 예제 | (RF템플릿)codeSnippets.ts(누락됐던 RandomForest 전체코드 export 템플릿 신설=DecisionTree/GradientBoosting 미러, 재현성 갭 해소), (예제)verify/datasets/bike_sharing_hourly.csv+Examples_in_Load/(UCI Bike Sharing 시간별 17,379행)·verify/pipelines/17_bike_demand_rf.json(RandomForest 회귀)·samples/Book_BikeSharing_DemandForecast.json+samples-metadata.json, (잔차진단 2-1)codeSnippets.ts+data_analysis_modules.py(EvaluateModel 회귀에 잔차 요약=mean/std/5수요약/3σ이상치, 결정적), (CV리포트 2-2)codeSnippets.ts+data_analysis_modules.py(SweepParameters 폴드별 mean±std·split점수 출력) (양쪽 앱) | docs/azure_ml_book/04_elston_based_improvements.md + 책자 제9부(28장) 기반. 전부 가산적·결정적(.6f, np.quantile 결정적). RandomForest 회귀 R²≈0.9422·RMSE≈42.72(test n=5214). verify:pipelines 16/16 PASS(양쪽, 신규 17 포함), build 성공(양쪽). 공통 코드(RF템플릿·잔차·CV·픽스처·샘플·데이터) 모두 byte-identical, JMDC 전용 차이(cohort)만 유지. 잔차 시각 차트(EvaluationPreviewModal)·시계열 특징공학·순열 중요도는 후속(04 문서 잔여 항목) |
| 2026-06-23 | Elston 개선 2차: 분위수 이상치 필터(2-5) | codeSnippets.ts(DataFiltering 템플릿 row 필터에 `quantile_above`/`quantile_below` 연산자 추가=df[col].quantile(value) 기준, 결정적), data_analysis_modules.py(filter_data 동일 가산), components/PropertiesPanel.tsx(DataFiltering 연산자 목록에 분위수 2종 추가), verify/pipelines/18_datafilter_quantile.json(자전거 cnt 5~95% 트리밍→RandomForest 회귀) (양쪽 앱) | docs/azure_ml_book/04 개선 2-5 구현(Elston식 0.20 분위수 이상치 트리밍). 훈련셋 한정 적용 권장(누수 방지, SplitData 뒤). 결정적(분위수·필터 모두 deterministic). 자전거 예제 17,379→15,689행 트리밍 확인. verify:pipelines 17/17 PASS(양쪽, 신규 18 포함), build 성공(양쪽). 공통 코드 4파일 모두 byte-identical |
| 2026-06-23 | Elston 개선 3차: 신규 FeatureEngineer 모듈(시계열·순환·상호작용, 2-3) | types.ts(FeatureEngineer enum), constants.ts(TOOLBOX_MODULES+DEFAULT_MODULES, operations 파라미터·data_in/out), codeSnippets.ts(FeatureEngineer 전체코드 템플릿: cyclical sin/cos·interaction a*b·trend 순번), data_analysis_modules.py(feature_engineer 함수), utils/generatePipelineCode.ts(MODULE_OUTPUT_VAR=engineered_data), components/PropertiesPanel.tsx(operations JSON 편집기+미리보기 stats), utils/pyodideRunner.ts(featureEngineerPython=인앱 실행), App.tsx(가산 else-if 분기로 인앱 실행), verify/pipelines/19_feature_engineer.json (양쪽 앱) | docs/azure_ml_book/04 개선 2-3 구현. 신규 데이터-변환 모듈(export+인앱 모두 지원, TransitionData 패턴 미러). 전부 결정적(sin/cos·곱·arange). 자전거 hr 순환+workingday×hr 상호작용+추세 추가→특징 17→21열, R² 0.9422→**0.9493** 개선 확인. verify:pipelines 18/18 PASS(양쪽, 신규 19 포함), build 성공(양쪽). 공통 코드(7파일+픽스처) 모두 byte-identical, App.tsx는 기존 분기 불변·가산만 |
| 2026-06-23 | Elston 개선 4차: 신규 FeatureImportance 모듈(순열 특징중요도, 2-4) | types.ts(enum), constants.ts(TOOLBOX+DEFAULT_MODULES: model_in+data_in→data_out, label_column·n_repeats), codeSnippets.ts(FeatureImportance 템플릿: sklearn permutation_importance, random_state=42, 중요도 내림차순 표·출력), data_analysis_modules.py(feature_importance 함수), utils/generatePipelineCode.ts(MODULE_OUTPUT_VAR=feature_importances), components/PropertiesPanel.tsx(label_column·n_repeats 편집기), App.tsx(가산 else-if: 인앱 안내 표시), verify/pipelines/20_feature_importance.json (양쪽 앱) | docs/azure_ml_book/04 개선 2-4 구현. **export 경로**에서 결정적 순열 특징중요도 계산(자전거: hr 1.366≫workingday 0.291≫temp 0.205…, holiday·windspeed 최하=가지치기 후보). 인앱은 선형 계수만 보관(트리 재예측 불가)→안내 표시, 실제 계산은 내보낸 Python에서(정직한 한계). verify:pipelines 19/19 PASS(양쪽, 신규 20 포함), build 성공(양쪽). 공통 코드 8파일+픽스처 byte-identical, App.tsx 가산만 |
| 2026-06-23 | Elston 개선 5차: 회귀 잔차 진단 시각 차트(2-1 완성) | utils/pyodideRunner.ts(generateResidualPlotPython 신설=잔차 vs 실제 산점도+잔차 히스토그램 base64 PNG, matplotlib), components/EvaluationPreviewModal.tsx(residualPlotImage 상태+useEffect+회귀 섹션 렌더) (양쪽 앱) | 04 개선 2-1의 시각 차트 부분 완성(요약 통계는 4차에 이미 구현). 기존 generateRegressionPlotPython 패턴 미러, 앱 표시용(내보낸 코드·verify 무관). build 성공(양쪽), 잔차 플롯 로직 standalone PNG 생성 확인. 공통 코드(함수+모달 효과·렌더) byte-identical |
| 2026-06-23 | 신규 PythonScript 모듈(2-7, 커스텀 코드·고급 게이트) | types.ts(PythonScript), constants.ts(TOOLBOX+DEFAULT, code 파라미터), codeSnippets.ts(getModuleCode 특수처리=사용자 코드 RAW 삽입, replacePlaceholders 우회), utils/generatePipelineCode.ts(MODULE_OUTPUT_VAR=scripted_data), components/PropertiesPanel.tsx(코드 에디터+AdvancedOnly 게이트+⚠️경고), utils/pyodideRunner.ts(runUserScriptPython 샌드박스), App.tsx(가산 분기), verify/pipelines/29_python_script.json (양쪽 앱) | docs/azure_ml_book/05 §3 구현. 'dataframe'→사용자 코드→'scripted_data'(없으면 통과). 보안: 고급 게이트 의무·Pyodide 샌드박스·60초 타임아웃·시드 고정 가이드. 결정적 스니펫 픽스처로 verify ML 27/27·JMDC 28/28 PASS, build 성공. 공통 코드 byte-identical. DFA 후순위(미적용) |
| 2026-06-23 | 재현성 원칙: 인앱 전용 분석 3모듈 export-Python화 | codeSnippets.ts(Correlation→df.corr 상관행렬, OutlierDetector→IQR 이상치, HypothesisTesting→scipy 검정 디스패치[t/카이제곱/ANOVA/KS/Shapiro/Levene]=인앱 performHypothesisTests와 동일 로직; 모두 결정적·dataframe 통과), verify/pipelines/26~28(iris) (양쪽 앱) | 기존 export가 "설정만 저장" 스텁이라 분석이 외부 재현 불가였음→실제 Python으로 교체(원칙 충족). verify ML 26/26·JMDC 27/27 PASS, build 성공. 3종 템플릿 byte-identical. DFA엔 해당 3모듈 없음 |
| 2026-06-23 | ★재현성 원칙 점검·복구: 내보내기 템플릿 갭 4종 수정 | codeSnippets.ts(LogisticRegression·SVM·LDA·NaiveBayes 전체코드 export 템플릿 신설; create_* 정합·결정적), constants.ts(LDA를 DEFAULT_MODULES에 추가=누락분), verify/pipelines/22~25(iris 분류 픽스처) (양쪽 앱) | 감사 결과 RandomForest급 잠복 버그 확인(이 4모델 배치 시 전체코드 빈 블록→NameError, 외부 재현 불가=Python 재현성 불변식 위반). 4종 모두 복구 → "모든 지도학습 분석 모듈이 검증 가능한 Python으로 export" 원칙 회복. verify:pipelines ML 23/23·JMDC 24/24 PASS, build 성공(양쪽). 템플릿 ML↔JMDC byte-identical. (DFA는 동일 3종 별도 수정; DFA 군집 패밀리 미완성은 05 문서에 기록) |
| 2026-06-23 | 인앱 RandomForest/GradientBoosting 정확도 수정(실제 sklearn 적합·예측) | utils/pyodideRunner.ts(fit/scoreRandomForestPython·fit/scoreGradientBoostingPython 신설=sklearn.ensemble, random_state=42 결정적, fit/scoreDecisionTreePython 패턴 미러), App.tsx(TrainModel 회귀·분류 RF/GB 분기 추가=난수 시뮬레이션 폴백 대체, ScoreModel 재적합 디스패치 조건+핸들러에 RF/GB 추가) (3개 앱: ML·JMDC·DFA) | RF/GB가 인앱 TrainModel 전용 분기 부재로 `Math.random()` 시뮬레이션 폴백(가짜·비결정적 계수·메트릭)에 빠지고 ScoreModel이 그 난수에 선형식을 적용하던 버그 수정(이전 "선형 근사" 진단은 부정확—DecisionTree/KNN/NN은 이미 정상이었음). 가산적·하위호환, export·verify 불변. 검증: build 성공(3앱), verify ML 27/27·JMDC 28/28·DFA 8/8 PASS, 실제 브라우저 Pyodide에서 RF R²≈0.99·GB≈0.999·feature importance 실제 관계 복원·ScoreModel 2회 재현 동일. 공통 코드 ML↔JMDC byte-identical, DFA는 score 함수 주석만 차이(scoreKNN 미러). **별개 발견(미해결):** SVM/LDA/NaiveBayes(3앱)·DecisionTree(DFA)는 ScoreModel이 호출하는 score 함수가 pyodideRunner에 없어 인앱 스코어링 크래시(export·verify는 정확). 상세 docs/azure_ml_book/05 §8 |
| 2026-06-24 | 샘플 데이터 자동 로드(데이터셋 레지스트리) + 연결선 초기 위치 버그 수정 | (수정1)utils/datasetRegistry.ts(신규: 하이브리드 데이터셋 레지스트리=파일명→CSV 본문 해석, 캐시→번들 public/examples-in-load.json→Supabase Storage 'datasets' 버킷 폴백; bindDatasetsToModules로 LoadData/XolLoading에 fileContent 자동 주입), App.tsx(handleLoadSample resetModules 직전 바인딩 패스+import), (수정2)components/Canvas.tsx(useLayoutEffect+useMemo로 모듈 구성 변경 시 페인트 직전 연결선 좌표 1회 강제 재계산) (4개 앱: ML·JMDC·life matrix new·DFA) | (수정1) 큐레이션 샘플(Book_*)이 LoadData에 파일명(source)만 담고 fileContent가 없어 실행 시 "No file content" 에러(예: Book_RestaurantRatings_Recommender). 데이터는 이미 public/examples-in-load.json에 있었으나 샘플 로드 경로와 미연결이었음→자동 바인딩으로 해소. Azure ML '등록된 데이터 자산' 방식 미러(이름으로 참조, 하이브리드 호스팅). 가산적·하위호환, Pyodide 실행/내보낸 Python/verify 불변(입력층 fileContent만 채움). 모델 저장은 기존대로 parameters(=fileContent 포함) 통째 저장→데이터 동봉 유지. (수정2) getPortPosition이 포트 DOM 등록(portRefs=useRef) 전 첫 렌더에서 moduleHeight 근사 폴백 좌표 사용, ref는 재렌더 미유발→모듈 선택 전까지 화살표 어긋남. 4개 앱 공통 버그. 검증: ML 실제 브라우저 Playwright(레스토랑 샘플 Load✓·Recommender✓ 끝까지 실행, 화살표 경로 초기=선택후 동일=측정좌표). 빌드 4앱 exit 0, verify ML 27/27·JMDC 28/28·DFA 8/8 PASS. 공통 코드 ML↔JMDC byte-identical, life matrix new(fileContentCache 모듈 부재→인라인 캐시)·DFA(LOADER_TYPES에 LoadClaimData 추가, fileContentCache 신설)는 적응 포팅. life matrix/DFA 샘플은 이미 fileContent 동봉→수정1은 가산적 안전망(no-op). **잔여(선택):** Phase B localStorage 쿼터 초과 폴백(대용량 동봉 시), Phase C Supabase 'datasets' 버킷 실제 업로드 |
| 2026-06-24 | 모델 저장 옵션(데이터 포함/제외·웹 등록·설명) + 쿼터 안전망(B) + Supabase 업로드(C) | utils/datasetRegistry.ts(EMBED_SIZE_LIMIT_MB=2·contentSizeMB·uploadDatasetToWeb=Supabase Storage 'datasets' upsert), components/SaveModelOptionsModal.tsx(신규: 로더별 포함/제외/웹등록 토글+데이터 설명+용량 경고), App.tsx(saveOptions/isSavingModel state, collectLoaderInfos·openSaveOptions·performModelSave·isQuotaError 헬퍼, My Work 저장+handleSaveAsSample을 모달 경유로 교체, 모달 렌더), components/PropertiesPanel.tsx(LoadData에 dataDescription textarea) (4개 앱) | 사용자 요청: 저장 시 데이터 선택적 포함, 대용량은 임베드 불가 안내 후 예제(웹)에 등록해 참조 사용, 데이터 위치·출처 설명 입력(포함 여부 무관). localStorage setItem try/catch로 쿼터 초과 시 데이터 유실 없이 안내(B). uploadDatasetToWeb로 'datasets' 버킷 업로드→resolveDatasetContent가 이름으로 해석(C, Phase A 바인딩과 연동). 가산적·하위호환, Pyodide/export/verify 불변. 검증: ML 실제 브라우저 Playwright(저장 옵션 모달=파일명·크기·포함/웹등록/설명 렌더→저장 시 localStorage에 fileContent 223B+설명+source 영속·모달 닫힘, PropertiesPanel 설명필드 표시). 빌드 4앱 exit 0, verify ML 27/27·JMDC 28/28·DFA 8/8 PASS. ML↔JMDC byte-identical(datasetRegistry 추가블록·SaveModelOptionsModal 전체·App 헬퍼·PropertiesPanel 필드), life matrix new(인라인 캐시·LoadData 단일·편집기는 ParameterInputModal)·DFA(LoadClaimData 로더 추가)는 적응 포팅. SaveModelOptionsModal은 4앱 자기완결 동일 |
| 2026-06-24 | UX 라운드: 실행시 데이터 폴백·Ctrl+C 텍스트복사·코드탭 게이트해제·라이트 미리보기색 (4앱) | App.tsx(LoadData 실행 시 fileContent 없으면 resolveDatasetContent로 자동 해석=레지스트리/웹 폴백; Ctrl+C/X 핸들러에 텍스트선택 가드→선택 텍스트는 브라우저 기본 복사 허용), components/PropertiesPanel.tsx(모듈별 '코드' 탭 AdvancedOnly 게이트 해제=항상 표시+select-text; 미리보기 탭/렌더함수 하드코딩 다크색→테마 대응) | 사용자 보고 대응: 샘플 데이터 미로드 에러(어떤 로드 경로든 실행 시 데이터 확보), 모듈 코드 텍스트 Ctrl+C 복사 불가(모듈복사 단축키가 가로채던 것), 코드가 고급게이트로 숨겨짐(과거처럼 노출), 라이트모드 미리보기 식별난. 가산적·하위호환, Pyodide/export/verify 불변. 브라우저 검증(레스토랑 실행·Ctrl+C defaultPrevented·코드탭 노출). ML↔JMDC byte-identical, life matrix(pipelineEngine 실행코어·ParameterInputModal)·DFA 적응 |
| 2026-06-24 | 모듈 설명 초보자 강화(🔰쉬운설명+📊분석방법) + 전반 디자인 검토 + 책자 모듈사전 부록 (4앱) | moduleDescriptions.ts(ModuleDescription에 beginner·analysisMethod 필드 신설, 61개 모듈 전부 작성=비유·일상어 초보자 설명+내부 알고리즘 평이 설명), components/ModuleDescriptionModal.tsx(🔰 beginner 최상단 강조+📊 method 섹션, tone 추가), 디자인 검토 8컴포넌트(AIPipeline 2종·EvaluateStat·RAG·ColumnPlot·Toolbox·SamplesModal·Evaluation 라이트모드 가독성), docs/azure_ml_book/ML_Auto_Flow_Booklet.md(부록 A+ 모듈사전 13카테고리×61모듈, 차례 갱신, 가산) | 사용자 요청: 모듈 설명이 초보자에게 어려움→역할/입력/출력/분석방법을 쉽게 대폭 보강. 가산적, 기존 role/input/output 유지. 브라우저 검증(설명 모달 beginner/method 렌더). ML 61개↔JMDC 61개 byte-identical+JMDC전용 7개(J1~J7) 작성, DFA는 시스템 신설(moduleDescriptions+모달+PropertiesPanel ⓘ버튼, 38개 모듈), life matrix는 moduleHelp.ts+입력모달 카드. 책자는 ML 단독(JMDC 책자 후속) |
| 2026-06-24 | 버그 수정 4종: TrainCluster 즉시실행·미니맵 이동·TrainModel 분류라벨·EvaluateModel 문자열라벨 | App.tsx(TrainClusteringModel feature_columns 지역변수 갱신 누락 수정=모듈 선택 없이 실행; TrainModel 분류 시 문자열/범주 라벨→정수 코드 인코딩), utils/pyodideRunner.ts(evaluateModelPython: LabelEncoder로 비수치 라벨/예측 정수화, model_type 외 비수치 라벨도 분류 처리), components/CanvasMinimap.tsx(드래그 핸들 '전체 보기'+offset transform+더블클릭 원위치) | 사용자 보고: ①TrainCluster가 KMeans 사전 선택해야만 실행(지역 const feature_columns가 빈 배열로 남던 버그) ②미니맵 고정 위치 ③Adult 분류 예제 Train 'No valid data rows'(숫자 라벨만 허용) ④그 수정으로 노출된 EvaluateModel '데이터 타입 오류'(문자열 클래스 astype(int)). 가산적·하위호환, EvaluateModel 수정은 인앱 전용(export·verify 불변). 브라우저 검증(Adult 전체 실행 정확도 산출·wholesale KMeans 바로 실행·미니맵 핸들). ML build 0, verify 27/27. ML↔JMDC byte-identical, DFA는 분류라벨·EvaluateModel만 적응(클러스터/미니맵 미존재), life matrix 미해당 |
| 2026-06-25 | 클러스터링 결과 2D 산점도(PCA 차원축소·클러스터 색칠) + 유사 위치 동시 처리 | components/ClusterScatterPlot.tsx(신규 공유: SVG 산점도 컴포넌트=클러스터별 색·노이즈(-1) 회색·범례 + useCluster2DProjection 훅=결정적 5000샘플링 후 Pyodide 투영), utils/pyodideRunner.ts(computeCluster2DProjectionPython 신설: 피처≥3 PCA(2,random_state=42)·=2 패스스루·=1 1D, np.nan_to_num 행보존), components/ClusteringDataPreviewModal.tsx(KMeans 뷰에 산점도 섹션+고유클러스터수, 모달 라우팅 PCA만 PCAView·KMeans/DBSCAN/계층형은 클러스터 할당 뷰로 정정+헤더 라벨), components/TrainedClusteringModelPreviewModal.tsx(학습 모델 KMeans 뷰에 할당 클러스터 색칠 산점도) + ClusterScatterPlot.tsx에 pickNumericFeatures(값 기반 수치 판정) (양쪽 앱) | 사용자 요청: KMeans 등 AssignClusters 결과가 표로만 보임→PCA로 2D 축소한 클러스터 색칠 산점도 추가. "유사 위치"=학습모델 미리보기 산점도 신설 + DBSCAN/계층형이 PCA 뷰로 잘못 라우팅되던 잠복버그 정정(이제 클러스터 색칠 산점도+분포 표시). **수치 피처 판정은 col.type 라벨이 아닌 값 기반(pickNumericFeatures)**으로 — wholesale 등 CSV type 오추론 데이터(피처가 비수치로 저장)에서도 동작(기존 클러스터별 통계량 0행 한계와 동일 원인 회피). 전부 **인앱 표시 전용**(기존 잔차/회귀 플롯 패턴 동일)→내보낸 Python·verify와 무관, Python 재현성 불변식 안전. 가산적·하위호환. 검증: build 양쪽 exit 0, verify ML 27/27·JMDC 28/28 PASS, 투영 코어 외부 Python 결정성 확인(PCA·2/1피처 분기·NaN), **실제 브라우저(wholesale KMeans 4클러스터)**: AssignClusters·TrainCluster 양 모달 모두 440점 PCA 산점도(PC1 46.0%·PC2 40.5%) 클러스터 색구분(파랑277·빨강95·초록58·주황10) 정상 렌더 스크린샷 확인. ClusterScatterPlot·모달 2종 ML↔JMDC byte-identical, pyodideRunner는 동일 함수만 삽입(JMDC J1~J7 보존). DFA·life matrix는 클러스터링 모달 부재→미해당 |
| 2026-06-26 | 테이블 출력 모듈 표별 CSV 다운로드 + EvaluateModel 임계값/ROC 강화(재현성 포함) | (CSV)utils/csvExport.ts(범용 `downloadRowsAsCsv(baseName,columns,rows)`+`CsvColumn`+`safeFileName`, UTF-8 BOM·전체행), components/TableDownloadButton.tsx(신규 공용 ⬇CSV 버튼·rows 비면 비활성), 표 출력 모달 11종에 표별 버튼=ClusteringDataPreviewModal(6표)·TrainedClusteringModelPreviewModal·DataPreviewModal·StatisticsPreviewModal·CorrelationPreviewModal·OutlierDetectorPreviewModal·NormalityCheckerPreviewModal·EvaluationPreviewModal·StatsModelsResultPreviewModal·ModelComparisonModal·PredictModelPreviewModal(파생/행렬 표는 columns/rows 즉석 build, slice 무관 전체행·원본 정밀도); (EvaluateModel ★재현성)codeSnippets.ts+data_analysis_modules.py(EvaluateModel/evaluate_model classification·binary에 **threshold 스윕 표 0.00~1.00 0.05간격 21행**[acc/prec/rec/f1·TP/FP/TN/FN]+**ROC 곡선 roc_curve 21점**+AUC; pos_score 우선순위=scored_data `*_Predict_Proba_1` 컬럼→trained_model predict_proba/decision_function 재도출→N/A, 결정적·.6f); utils/pyodideRunner.ts(evaluateModelPython 인앱: 하드라벨 예측이어도 df의 `*_Predict_Proba_1` 자동감지→y_pred_proba 보강해 슬라이더/ROC/AUC 실작동); components/EvaluationPreviewModal.tsx(중복 정리=상단 Selected Threshold 패널을 thresholdTable>0일 때만·혼동행렬 헤더 threshold 조건부·하단 TP/FP/TN/FN 중복목록 제거) (양쪽 앱) | 사용자 요청 3건: ①테이블 출력(AssignClusters 등)에 다운로드 부여+유사 표 모듈 전반 ②EvaluateModel 임계값 바 조절 시 정확도/재현율 변화·결과 중복 정리·혼동행렬 기반 ROC/AUC ③"지금 작업 내용을 Python 코드에서도 산출"(최상위 재현성 불변식). 근본원인: ScoreModel이 Predict(하드라벨)+`{label}_Predict_Proba_1`(확률) 둘 다 내나 EvaluateModel 기본 prediction_column=Predict라 인앱 threshold 스윕/ROC 미생성→폴백 지표 중복. CSV는 인앱 표시 전용(export/verify 무관). threshold 스윕/ROC export는 결정적(고정 모델·데이터)→재현성 충족. 검증: build ML/JMDC exit 0, verify ML 27/27·JMDC 28/28 PASS(2회 byte-identical), 13_book_adult_clf 단독 export 실행=스윕 21행 임계값별 실변화(0.50 prec=0.812582 등)·ROC 21점·**사다리꼴 AUC=0.854369가 스칼라 ROC-AUC와 일치**·2회 동일. 공통 코어(TableDownloadButton·csvExport 추가블록·codeSnippets/data_analysis_modules EvaluateModel 블록·pyodideRunner 가산) ML↔JMDC byte-identical, 모달은 변수 연결 차이만. DFA·life matrix 후속(미적용) |
| 2026-06-26 | 신규 "모델 분석보고서"(ModelAnalysisReport) 모듈 + model-analysis-report 스킬 + 첫 HTML 보고서 | (스킬)`.claude/skills/model-analysis-report/`(SKILL.md=방법론·입력우선순위[사용자PDF/텍스트→파이프라인 메타데이터→**웹 리서치/AI 일반지식 폴백**]·HTML 템플릿·AI 프롬프트·결정적 폴백·고급게이트·모듈 청사진 + templates/report_template.html); (첫 보고서)docs/reports/adult_census_income_decisiontree_분석보고서.{md,html,pdf}(자기완결 HTML=앱 라이트 스타일, make-pdf로 PDF, 헤드리스 렌더 검증); (모듈)types.ts(ModuleType.ModelAnalysisReport+ReportContext/ModelReportOutput), utils/modelReport.ts(신규=gatherReportContext 역방향 그래프 수집·buildModelReportHtmlFallback 결정적 HTML·escapeHtml), utils/pdfText.ts(신규=pdfjs-dist 텍스트추출), lib/aiHelpers.ts(generateModelReportHtml=키없으면 폴백·있으면 gemini-2.5-pro→flash→폴백·`<html`검증·`<script`차단), constants.ts(TOOLBOX+DEFAULT_MODULES: report_in 입력·출력없음·말단·파라미터 title/extra_info/extra_pdf_text/use_web_research), components/Toolbox.tsx(Operations 노출), components/PropertiesPanel.tsx(편집기+PDF업로드+'보고서 생성' AdvancedOnly 게이트), components/ModelReportPreviewModal.tsx(신규=iframe srcDoc 렌더·HTML 다운로드·인쇄·source 배지·열람 무게이트), App.tsx(실행분기 가산 else-if+isAdvancedUnlocked 게이트+미리보기 라우팅+모달), codeSnippets.ts(getModuleCode no-op), moduleDescriptions.ts (양쪽 앱) | 사용자 요청: 모델 개발 후 개발과정을 HTML 문서로 남기는 모듈을 AI로 생성, 파이프라인 끝에 위치, 입력으로 PDF 등 추가정보, 입력 없으면 인터넷 조사/일반지식으로 보강, 스킬로 구성, "모델 분석보고서" 명칭, **실행은 고급사용자만·열람은 누구나**. ★**문서화(메타) 모듈이라 데이터분석 아님→codeSnippets/export/verify 비대상**(Python 재현성 불변식 무관). 키 없을 때 결정적 폴백으로 결과 비지 않음. 검증: build ML/JMDC exit 0, verify ML 27/27·JMDC 28/28 PASS(모듈 verify 영향 0), 실브라우저(ML): 툴박스 노출·배치·고급게이트 실행차단(이중)·키없이 폴백 HTML 자기완결 렌더·모달 wiring·콘솔 0 PASS(고급 해제는 ADVANCED_PASSWORD_HASH 미설정 환경상 UI 생성 1구간만 코드+렌더 대체검증). 신규 3파일(modelReport·pdfText·ModelReportPreviewModal)·aiHelpers/types 추가블록 ML↔JMDC byte-identical, JMDC J1~J7·cohort 보존. 스킬·docs는 ML 단독. DFA·life matrix 후속(미적용) |
| 2026-06-26 | AI 백엔드 Gemini→Claude(Anthropic) 전환 + 목적별 모델 티어 (4개 앱) | (SDK)package.json `@google/genai`→`@anthropic-ai/sdk`; (코어)lib/aiClient.ts(`GoogleGenAI`→`Anthropic{apiKey,dangerouslyAllowBrowser:true}`, `getGeminiClient`→`getClaudeClient`, `generateClaudeText`/`extractText` 신설, `CLAUDE_FAST="claude-haiku-4-5"`/`CLAUDE_CAPABLE="claude-sonnet-4-6"`, testApiKey=Claude ping+SDK 타입 에러분류, 키 `gemini_api_key`→`anthropic_api_key`, 이벤트 `anthropic-api-key-changed`, dev폴백 `ANTHROPIC_API_KEY`), lib/aiHelpers.ts(`runPrompt(prompt,model=FAST,maxTokens=8192)`·`streamPrompt`=messages.create/stream, generateModelReportHtml=CAPABLE 단일호출+결정적 폴백·SDK 429 자동재시도); (호출부)App.tsx(모듈추천·파이프라인생성=CAPABLE, Gemini responseSchema→JSON-only 프롬프트+파싱), components/PropertiesPanel.tsx(설명=FAST·추천=CAPABLE+stripJsonFence), 미리보기/오류 모달 다수(generateClaudeText·"Claude API 키" 문구), ApiKeySettingsModal.tsx(라벨·sk-ant- 플레이스홀더·console.anthropic.com 링크); vite.config.ts(define `ANTHROPIC_API_KEY`+manualChunks), types.ts·moduleDescriptions.ts·utils/modelReport.ts·ModelReportPreviewModal.tsx(라벨 Claude) (4개 앱: ML·JMDC·DFA·life matrix new) | 사용자 요청: Gemini API→Claude API 전환, 사용 수준을 목적에 맞게 조절, 4개 앱 전부. **티어**: 빠른 보조(코드해설·결과해설·오류수정)=Haiku 4.5, 생성·추론(AI 파이프라인·모델 분석보고서·모듈 추천·JSON 구조화)=Sonnet 4.6. 브라우저 직접호출은 `dangerouslyAllowBrowser:true`(기존 Gemini도 브라우저 키 노출 방식이라 보안 동등). Gemini `responseSchema`→"오직 JSON" 프롬프트+코드펜스 제거 파싱으로 대체. AI는 인앱 전용→Pyodide/내보낸 Python/verify 무관(불변식 안전). 검증: 실코드 @google/genai 0건(ML/JMDC/life matrix; DFA는 멀티프로바이더 폴백만 동적import 보존), build 4앱 exit 0, verify ML 27/27·JMDC 28/28·DFA 8/8 PASS. lib/aiClient.ts·aiHelpers.ts·ApiKeySettingsModal.tsx ML↔JMDC byte-identical, JMDC J1~J7·cohort 보존. DFA는 기존 멀티프로바이더(`utils/aiClient.ts`)에 anthropic 기본화+13개 직접호출 통합, life matrix new는 `ApiKeyContext` ensureClient→Anthropic. **런타임 주의: 유효한 `sk-ant-` 키 필요**(사용자 설정 모달/.env). 실제 Claude 호출은 키 부재로 미실행(빌드·SDK 사용법 검증) |
| 2026-06-27 | Claude API 실호출 검증(A) + .env 키 정리 + EvaluateModel CSV를 DFA에 동기화(B) | (A 검증)`sk-ant-` 실키로 두 티어 실호출 확인=FAST `claude-haiku-4-5` ping OK·CAPABLE `claude-sonnet-4-6` 스트리밍이 `<html>`포함·`<script>`없는 유효 HTML 생성(=generateModelReportHtml과 동일 호출). 과거 E2E의 400은 stale 테스트환경 아티팩트(잘못된 키→401 확인). (A 정리)ML `.env`의 `ANTHROPIC_API_KEY=AIzaSy…`(전환 잔재 Gemini 키, dev폴백 시 401)를 빈 값으로 교체(실키는 `.env.local`). JMDC `.env`는 죽은 `GEMINI_API_KEY`만 존재·`.env.local` 키 없음→설계상 설정모달 입력 의존(기능장애 아님, 보고만). (B 동기화)DFA-Auto-Flow에 `utils/csvExport.ts`·`components/TableDownloadButton.tsx` 신설(ML과 내용 동일, EOL만 차이) + `components/EvaluationPreviewModal.tsx` 2개 표(임계값 통계·혼동행렬)에 ⬇CSV 버튼 부착. life matrix new는 EvaluationPreviewModal 부재→해당없음. CSV는 인앱 표시 전용(export/verify 무관). 검증: DFA build exit 0·verify 8/8 PASS, csvExport/TableDownloadButton ML↔DFA 내용 동일 확인. (C)모델 분석보고서 모듈 최종 통과: ML build exit 0, 폴백 HTML 결정적 렌더(8460자·`<!DOCTYPE>`·`<script>`없음·외부URL 0·실측 메타 바인딩), `_workspace` QA 잔재 57개를 `_qa_archive/`로 정리(설계노트 *.md 6개 상단 유지) |
| 2026-06-28 | '내 작업' 메뉴 검증 + 신규 "마지막 작업 불러오기"(localStorage 영구 저장) | hooks/useAutoSave.ts(localStorage `mlAutoFlow_lastWork`+경량 `mlAutoFlow_lastWork_meta` 신설: 디바운스 800ms 영구 저장, 데이터 본문 4MB까지 보존·초과/쿼터 시 fileContent=undefined로 구조만 저장→실행 시 source 자동 재로드, `loadLastWork`/`getLastWorkMeta` export; 기존 세션 자동저장·마운트 세션복원은 불변), App.tsx(formatSavedAt 헬퍼·lastWorkMeta state·메뉴 열림 시 메타 새로고침 effect·'내 작업' 드롭다운 최상단 🕘'마지막 작업 불러오기' 버튼=loadLastWork→resetModules/_setConnections로 복원, 저장시각·모듈수 표시, 없으면 비활성) (양쪽 앱) | 사용자 보고: '마지막 작업 불러오기'가 가끔 안 됨. 근본원인=유일 복원수단인 자동저장이 **sessionStorage**(브라우저 닫으면 소실)+TTL 1h였고 사용자가 직접 누를 버튼 부재. 해결=localStorage 영구 스냅샷+명시적 메뉴 버튼(가산적, 세션 자동저장·initialModel 경로 불변). UI 전용→Pyodide/내보낸 Python/verify 무관(재현성 불변식 안전). 검증: ML build exit 0·verify 27/27 PASS, **실브라우저 E2E**(모듈 2개 추가→localStorage 기록 확인→sessionStorage clear+리로드[브라우저닫기 모사]→버튼 '방금 전·2개 모듈' 활성→클릭→캔버스 2모듈 복원, 콘솔 에러 0). useAutoSave.ts ML↔JMDC byte-identical, App.tsx 삽입블록 동일 문자열. **메뉴 검토(추가/삭제):** 추가=마지막 작업 불러오기(완료). 유지=파일에서 불러오기·현재 모델 저장·초기 화면으로 설정·저장모델 목록(정상 동작 확인). 권고(미적용)=① '초기 화면으로 설정'은 신규 기능과 의미 일부 중첩(자동 시작 vs 수동 복원)→라벨 명확화 검토 ② 저장모델 타임스탬프 부재(이름키 덮어쓰기)→정렬/시각 표기 개선 여지. DFA·life matrix 후속(미적용) |
| 2026-06-28 | '마지막 작업 불러오기' DFA·life matrix 앱으로 확대 적용 | (DFA-Auto-Flow)hooks/useAutoSave.ts 신설(**ML과 byte-identical**)+App.tsx(useAutoSave 연결·🕘버튼·formatSavedAt/lastWorkMeta/effect); (life matrix flow new)App.tsx(자체 키 `lifeMatrixFlow_lastWork` 모듈레벨 헬퍼+디바운스 저장 effect+🕘버튼, 기존 `lifeMatrixFlow_initialState` 자동복원과 분리) | 앞 행(ML·JMDC) 기능을 나머지 2개 앱에 적응 포팅(사용자 요청 "DFA·life matrix에도 동일 적용"). DFA=자동저장 전무→useAutoSave 도입(새로고침 복원도 함께 확보, 훅 byte-identical), life matrix=initialState 자동복원은 있었으나 명시적 버튼·연속 저장 부재→분리 키로 추가(productName·showToast 등 LM 아키텍처 적응 자체 구현). UI 전용→Pyodide/내보낸 Python/verify 무관. 검증: DFA build exit 0·verify 8/8 PASS, life matrix build exit 0·verify 6/6 PASS, **양쪽 실브라우저 E2E**(디바운스 저장 갱신·🕘버튼 활성·클릭→복원·콘솔 JS 에러 0). 결과: 4개 앱(ML·JMDC·DFA·life matrix) 전부 '내 작업 > 마지막 작업 불러오기' 일관 제공. 각 앱 CLAUDE.md에도 개별 기록 |
| 2026-06-28 | '내 작업' 메뉴 라벨 명확화(4개 앱) | App.tsx 메뉴 항목: **'초기 화면으로 설정' → '시작 화면으로 지정'**(부제 "앱을 다시 열 때 이 모델로 시작" + title툴팁 "자동 복원되는 '마지막 작업'과 다름"), '현재 모델 저장'(부제 "이 브라우저에 저장 · 아래 목록에 추가"), '파일에서 불러오기'(부제 "내 PC의 저장 파일(.json) 열기") — 각 버튼 2줄 구조화. 관련 addLog/토스트 문구도 "시작 화면으로 지정되었습니다…"로 변경 (ML·JMDC·DFA·life matrix) | 사용자 요청: 신규 '마지막 작업 불러오기'(자동·최근 복원)와 '초기 화면으로 설정'(수동·시작 기본값 고정)의 의미 혼동 해소. 순수 라벨/문구·JSX 텍스트 변경(동작·키·저장로직 불변)→Pyodide/내보낸 Python/verify 무관. 검증: 4개 앱 build exit 0, ML 실브라우저 메뉴 렌더 확인(4개 항목 부제 표시·'시작 화면으로 지정', 콘솔 에러 0[favicon만]). ML↔JMDC 변경 블록 동일 문자열, DFA(className 차이만)·life matrix(flex-col·고급게이트 구조 적응) 적응 |
| 2026-07-04 | 모바일 헤더 리디자인(햄버거 드로어+모바일 액션 바) + 4앱 디자인 일관성 통일 | App.tsx(isMobileMenuOpen state 신설·isLeftPanelVisible 초기값 innerWidth>=768·Row1 우측 모바일 햄버거[md:hidden]·신규 모바일 액션 바[flex md:hidden: 모듈토글/샘플/전체 실행/속성]·Row2/Row3 hidden md:flex 데스크톱 전용화·모바일 드로어[백드롭+w-72: 파일/내 작업/편집·보기/분석/고급기능 5섹션, 기존 핸들러·isAdvancedUnlocked 게이팅 그대로 재사용]·Row3 극소 폰트 text-[5px]~[8px]→text-[11px]/배지 [9px] 정규화[overflow는 내 작업 드롭다운 클리핑 방지 위해 우측 그룹에만]·좌측 툴박스 모바일 오버레이[absolute md:static z-30+백드롭]·우측 패널 max-w-[100vw]·XMarkIcon import 보강) (양쪽 앱; DFA·LM 적응 포팅) | 사용자 요청: 모바일에서 상단 버튼이 다 안 보이고 복잡→실행에 바로 필요한 버튼만 유지+나머지는 햄버거로, 4개 시스템 디자인·배치 일관성 통일(기능 불변). md(768px) 브레이크포인트. 순수 UI(핸들러·게이팅 로직 불변)→Pyodide/내보낸 Python/verify 무관(재현성 불변식 안전). 스펙 `_workspace/1_design_spec.md`(ML=표준). DFA=라벨 한국어화(샘플/내 작업/폴더 설정/불러오기/저장/전체 실행)+전체 실행 Row2 끝 이동+샘플 라이트모드 클래스 보강+console.log 제거+zIndex:9999→z-50, LM=고급기능 토글 Row1 그라디언트→Row3 우측 ML 표준(amber 잠금/green 해제)+PPT·재계산은 SlideReportButton/RecomputeExportButton 컴포넌트 드로어 재사용, 각 앱 아키텍처 적응. 검증: build 4앱 exit 0, verify ML 27/27·JMDC 28/28·DFA 8/8·LM 6/6 PASS, **Playwright 4앱 실브라우저**(375×667: scrollWidth 375 초과 0·액션 바/드로어 렌더·테마 전환 실동작·툴박스 오버레이+백드롭·콘솔 에러 0 / 1280×800: 3줄 헤더 유지·모바일 요소 숨김·내 작업 드롭다운 무클리핑·샘플 로드 스모크) 4앱 각 10/10 PASS. ML↔JMDC 변경 블록 byte-identical(git apply --check 검증). QA 발견·수정: XMarkIcon import 누락(ML·JMDC, esbuild 특성상 build로 미검출→드로어 크래시)→import 추가 후 재검증 통과 |
| 2026-07-04 | tsc --noEmit 빌드 게이트 신설 + 잠복 타입 에러 115건 전량 해소 (4개 앱) | package.json("typecheck" 스크립트 신설·build를 `tsc --noEmit && …`로 게이트), tsconfig.json(types→["vite/client"]=ImportMeta.env+node iterator 전역 오염 해소, server/verify/scripts/_workspace/vite.config.ts 등 번들 밖 exclude), env.d.ts(신규: dev process.env 폴백 타입), types.ts(XoL/Diversion/EvaluateStat 출력 스텁 7종+outputData 유니언, 중복 CorrelationMatrix→CorrelationMethodResult 개명, StatsModelFamily·modelFamily·shapeData 넓히기), App.tsx·constants.ts·utils/pyodideRunner.ts(withTimeout<T=any>)·utils/pcaCalculator.ts·components 9종(Canvas/ComponentRenderer/DataPreviewModal/ErrorBoundary[declare state·props]/ExcelInputModal/OutlierDetectorPreviewModal/PropertiesPanel/SpreadViewModal/TrainedModelPreviewModal) — 전부 타입 전용(옵셔널 필드·캐스트·declare, 제어 흐름·값·enum 값 불변) | 직전 모바일 헤더 QA에서 XMarkIcon 누락이 esbuild(vite build 타입 미검사)로 미검출→드로어 크래시였던 재발 방지(사용자 요청). 이 부류를 빌드 단계에서 차단. 검증: ML tsc 0(115→0)·build exit 0·verify 27/27 PASS(pyodideRunner·types 등 런타임 파일 수정에도 Python 재현성 불변). JMDC 동일 이식(에러 목록 동일, git apply 14파일+types.ts 수동 6블록, JMDC 전용 추가 수정 0건, tsc 0·build 0·verify 28/28, env.d.ts byte-identical·J1~J7 보존). DFA 별도 53건 해소(server JS 구문오류가 tsc 전체를 가리던 것 exclude 후 발현: FitResult error? 옵셔널 21건·ErrorBoundary 7건 등, tsc 0·build 0·verify 8/8). LM 20건 해소(vite/client·_workspace exclude·allowedHosts 'all'→true 등, tsc 0·build 0·verify 6/6). **주의**: 4앱 모두 @types/react 미설치라 React 표면은 any로 통과(완전 타입 안전은 후속 별도 작업). 타입 정리 중 발견한 잠복 런타임 의심 2건은 현 상태 보존만(수정 안 함): ①fitSVMPython 인자 위치 어긋남 ②LDA 분기의 미존재 ModuleType.LinearDiscriminantAnalysis 참조 — 후속 검토 항목 |
| 2026-07-04 | 잠복 런타임 버그 2건 수정: 인앱 SVM 학습 크래시 + LDA 사문(死文) 분기 활성화 | (SVM)App.tsx(TrainModel 회귀 분기 fitSVMPython 호출에 누락됐던 probability 인자 `false` 삽입 — 기존엔 ordered_feature_columns가 probability 자리·60000이 featureColumns 자리로 밀려 featureColumns.forEach TypeError로 회귀 SVM 학습 항상 실패; 분류 분기 2곳은 원래 정렬 정상), utils/pyodideRunner.ts(fitSVMPython 템플릿 `p_probability = ${probability}`→`${probability ? "True" : "False"}` — JS boolean이 소문자 true/false로 렌더되어 Python NameError로 분류 SVM 학습도 실패하던 것 수정, 타 fit* 함수 관례 정합); (LDA)App.tsx 4곳·components/ComponentRenderer.tsx·PropertiesPanel.tsx 3곳·TrainedModelPreviewModal.tsx의 미존재 enum `(ModuleType as any).LinearDiscriminantAnalysis`(=undefined) 참조를 `ModuleType.LDA`로 교체(학습 분기 2곳은 좁힘 회피용 `(modelSourceModule.type as ModuleType)` 캐스트 — 인접 SVM 분기 관례), constants.ts(DEFAULT_MODULES의 죽은 중복 LDA 항목 삭제 — 파라미터 동일한 정상 ModuleType.LDA 항목 존치) (양쪽 앱) | 직전 tsc 게이트 작업에서 발견·보존만 했던 의심 2건을 사용자 요청으로 검증 후 수정. 검증 결과 둘 다 실재: ①인앱 SVM은 회귀(인자 밀림 TypeError)·분류(Python NameError) 모두 학습 실패 후 에러 경로였음 ②LDA는 fitLDAPython 학습 분기·scoreLDAPython 재적합 디스패치가 undefined 비교로 전부 사문 → 폴백/레거시 선형 경로. DFA는 해당 없음(enum에 LinearDiscriminantAnalysis 정의·fitSVMPython에 probability 매개변수 없음 — 확인 완료). **인앱 전용 수정**(codeSnippets/export/verify 무관 — export 경로의 SVM/LDA 템플릿은 원래 정상). 검증: tsc 0·build exit 0·verify ML 27/27·JMDC 28/28 PASS, **실브라우저 Pyodide 4/4 PASS**(iris SVM 분류 prob=False/True 각 Acc 1.0, BostonHousing SVM 회귀 MSE 53.42·R² 0.286 실산출[이전엔 학습 자체 불가], iris LDA 학습→예측→평가 Acc 1.0 — "Python으로 LDA 모델 훈련/예측 완료" 로그로 실경로 진입 확인), 콘솔 page error 0. ML↔JMDC 동일 수정(공통 코드) |
| 2026-07-04 | 사문(死文) 참조 일괄 정리: 미존재 ModuleType 11곳 제거 | App.tsx(isLoaderModuleType의 `\|\| t === XolLoading` 절 제거), components/Canvas.tsx(getPortPosition의 XolCalculator·SplitByThreshold/ApplyThreshold 특수 포트좌표 분기 2블록 제거), components/ComponentRenderer.tsx(getInputPortStyle 동일 2블록 제거), components/PropertiesPanel.tsx(LoadData case의 XolLoading 병기 라벨·조건 3곳 단순화) (양쪽 앱) | XolLoading/XolCalculator/SplitByThreshold/ApplyThreshold는 ML·JMDC enum에 없는 타 앱(보험/DFA 계보) 잔재 — 전부 `undefined` 비교라 실행 불가한 사문(직전 tsc 게이트 작업에서 `(ModuleType as any)` 캐스트로 보존했던 것을 사용자 요청으로 제거). 동작 불변(제거 분기는 미존재 모듈 전용, 남은 문자열 키[codeSnippets 템플릿 맵·datasetRegistry LOADER_TYPES]는 enum 비교 아님·무해). DFA는 XolCalculator 등 3종이 실존 모듈이라 XolLoading 2곳만 제거(별도 행), JMDC는 ML과 동일 11곳. 검증: 잔여 `(ModuleType as any)` 3앱 0건, tsc 0·build exit 0·verify ML 27/27·JMDC 28/28·DFA 8/8 PASS, **실브라우저 스모크 3/3 PASS**(automobile 샘플: 연결 화살표 좌표 정상·LoadData 속성 패널 File Name/예제 목록 정상·전체 실행 완주·콘솔 에러 0). 변경 블록 ML↔JMDC 동일(잔여 diff는 기존 의도 차이[JMDC description prop·공백 1줄]뿐) |
| 2026-07-04 | @types/react 19 도입: React 표면 완전 타입화 + 잠복 런타임 버그 수정(PCA 산점도 미렌더) | package.json(+lock: @types/react·@types/react-dom devDeps), App.tsx(globalThis.MouseEvent·addLog "WARNING"→"WARN" 2곳), components/Canvas.tsx(SVG title 스프레드 캐스트), components/ClusteringDataPreviewModal.tsx(**PCAClusteringView 포인트 forEach→map**), CorrelationPreviewModal.tsx(availableMethods 캐스트 2곳), ErrorBoundary.tsx(불필요해진 declare 3줄 제거), PropertiesPanel.tsx(PropertyInput min/max 수신·modelTypeDisplay:string), SamplesManagementModal.tsx(ManagementSample[] 캐스트) (양쪽 앱; DFA·LM 적응 포팅) | 직전 tsc 게이트 라운드의 예고 후속("4앱 @types/react 미설치→React 표면 any"). 대부분 타입 전용이나 타입화가 **실 런타임 버그를 노출**: ①ML/JMDC PCA 산점도 포인트가 JSX `{rows.forEach(...)}`(반환 undefined)로 전혀 렌더되지 않던 것→map ②DFA useModalCopy '복사' 버튼이 React 합성 이벤트에 없는 stopImmediatePropagation 직접 호출로 TypeError→`e.nativeEvent` 경유(복사 실작동 회복) ③LM handleLoadSample이 SampleData 객체 경로 미지원(이름 검색 절대 불일치)→객체 직접 수용. DFA는 22건 별도 해소(setFolderSamples 캐스트·PropertySelect value:string|number 확대·DataPreviewModal Connection 캐스트 2곳 등; **DFA·LM은 pnpm 저장소**—npm install은 .pnpm 혼재 트리에서 arborist 크래시, pnpm 사용·LM은 pnpm-lock 동기화). 검증: 4앱 tsc 0·build exit 0·verify ML 27/27·JMDC 28/28·DFA 8/8·LM PASS, **실브라우저 E2E 2건 PASS**(ML: iris→PCA→FitPCA→Transform 실행 3/3→PCAView 산점도 circle 143개 렌더[수정 전 0개]·PC1 92.8%·콘솔 0 / DFA: 12모듈 파이프라인 12/12 Success+Statistics 추가 연결 실행→미리보기 우클릭 '복사'→클립보드 848자·메뉴 정상 닫힘·page error 0). ML↔JMDC 공통 5파일 byte-identical(Canvas 기존 잔여 차이만)+App/PropertiesPanel 삽입블록 동일. **기존 발견(미수정, _workspace/13 노트)**: DFA LoadClaimData→Statistics 직결 시 ClaimDataOutput 미언랩 에러(SelectData 경유는 정상)·DFA 'XoL Experience' 샘플 연결정보 불량(Invalid connection)·ML '파일에서 불러오기'가 샘플 포맷(fromModuleIndex) JSON을 raw set→Canvas 크래시(저장 포맷은 정상) |
| 2026-07-05 | 잔존 버그 3건 수정 + '내 작업' 드롭다운 z-index 정규화 | App.tsx(handleLoadSample 샘플 객체 직접 수신[string\|Record + displayName] + 신규 loadWorkFromFileContent 헬퍼[런타임 포맷=raw 복원 / 샘플 포맷=handleLoadSample 변환 재사용 / 불명=WARN] + 파일 불러오기 핸들러 2곳[드롭다운·드로어] 헬퍼 교체 + 드롭다운 zIndex:9999→z-50) (양쪽 앱; DFA 동일 적응+전용 2건, LM은 기수정으로 안전—무변경) | 직전 라운드(_workspace_archive/2026-07-04_react_types_round/13) 미해결 3건 해소. ①ML/JMDC/DFA '파일에서 불러오기'가 샘플 포맷(fromModuleIndex·id 없는 모듈) JSON을 raw set→Canvas `c.from` 크래시 → 형식 감지 후 기존 샘플 변환 경로 재사용(알 수 없는 형식은 크래시 대신 경고) ②DFA Statistics가 LoadClaimData 직결 시 ClaimDataOutput 미언랩 에러 → SettingThreshold 분기 미러 언랩(가산) ③DFA 'XoL Experience'(Supabase 시드 .mla) 로드 실패 → DFA 샘플 연결 변환기 이중 형식(fromModuleIndex/from.moduleId) 지원+불량 연결 throw 대신 WARN 스킵(ML 표준 미러). 전부 인앱 UI 전용(Pyodide/내보낸 Python/verify 불변). 검증: 4앱 tsc 0·build exit 0·verify ML 27/27·JMDC 28/28·DFA 8/8·LM 6/6 PASS, **실브라우저 E2E 5건 PASS**(ML 샘플포맷 로드 8모듈+연결 렌더[수정 전 크래시]·ML 저장포맷 회귀·DFA 파일 로드 객체 경로·DFA Statistics 직결 2/2 Success·DFA from/to 변환 로드 정상), 콘솔 page error 0. ML↔JMDC 삽입 블록 동일 문자열. 잔여 패리티 권고(미적용): DFA EvaluateModel threshold/ROC·ModelAnalysisReport DFA/LM 포팅·TableDownloadButton 확산. 상세 _workspace/2_qa_report.md |
| 2026-07-05 | ★분류 라벨 인코딩 반전 잠복 버그 수정(3앱 공통) — 후속권고 라운드 중 발굴 | App.tsx TrainModel 라벨 인코딩: 첫 등장 순서 코드 부여(`nextLabelCode++`)→**사전 스캔+정렬 순서 부여**(수치 라벨은 수치 정렬, sklearn LabelEncoder 정합·결정적) (ML·JMDC·DFA 동일 블록) | 훈련 데이터 첫 행 라벨이 1이면 {1→0, 0→1}로 학습되어 ScoreModel `Proba_1`이 실제 P(class 0)·hard Predict·EvaluateModel 혼동행렬 전부 반전되던 잠복 버그(이진 로지스틱 E2E에서 정확도 0.267[반전 시 0.733]·AUC 0.1111로 발현). 인앱 전용(export 경로는 sklearn 직접 학습이라 원래 정상, verify 불변). 검증: 3앱 tsc 0·build exit 0·verify ML 27/27·JMDC 28/28·DFA 8/8 PASS, **실브라우저 E2E**(합성 이진 60행 LoadData→…→EvaluateModel 7모듈): ML·DFA 동일 픽스처에서 Accuracy 0.7333·ROC AUC 0.8148·AP 0.7650 **양앱 값 일치**, Proba_1 단조 방향 정상. 삽입 블록 3앱 동일 문자열. 한계 기록: 다중분류 비연속 수치 라벨은 코드≠원값(기존 설계 동일). 상세 _workspace/3_followup_round_qa.md |
| 2026-07-05 | 분류 라벨 경로 완결(문자열/비{0,1} 라벨 원 라벨 복원) + 저장 모델 타임스탬프 | types.ts(TrainedModelOutput.classLabels?: string[] 신설), App.tsx(①TrainModel 제네릭 계수 경로가 classLabels[코드 순서=정렬 순서] 저장 ②ScoreModel 계수 경로가 코드 예측(0..k-1)을 원 라벨로 복원[문자열은 문자열·수치는 원값 — {1,2} 비연속도 복원, Predict 컬럼 타입 갱신] ③myWorkModels 저장 시 savedAt 기록+목록 최신순 정렬+"N개 모듈 · N분 전" 표기[드롭다운·드로어, 하위호환]), utils/pyodideRunner.ts(evaluateModelPython: 이진인데 클래스 집합≠{0,1}이면 정렬상 두 번째 클래스=1 기준 0/1 정규화 — 스윕·ROC 정합) (양쪽 앱; DFA 동일 적응+전용 4건 별도 행) | 직전 라운드 잔여 리스크 해소: 계수 모델(로지스틱)의 Predict가 클래스 코드로 노출되어 문자열 y_true와 EvaluateModel LabelEncoder가 4클래스로 인코딩→전 지표 붕괴하던 경로 + 비{0,1} 수치 라벨 스윕 어긋남. 인앱 전용(export/verify 불변). 검증: tsc 0·build exit 0·verify ML 27/27·JMDC 28/28 PASS, **실브라우저 E2E**: 문자열 이진('no'/'yes'+로지스틱) 7/7 Success·Predict 원 라벨 복원·Acc 0.7333/AUC 0.8148/AP 0.7650(동일 시드 수치 라벨 실험과 완전 동일=정답), 3클래스 문자열(+DT 재적합 경로) 7/7·Predict 원 클래스·지표 1.0, 타임스탬프 정렬/표기/하위호환(주입 3건). ML↔JMDC 삽입 블록 동일 문자열. 잔여 한계 기록: 다중분류 혼동행렬 UI 이진 전용(지표는 정상)·구버전 저장 모델은 재학습 필요·다중분류 계수 경로는 기존 한계(재적합 모델 권장). 픽스처 3종+상세 _workspace/4_label_path_round_qa.md |
| 2026-07-05 | 폴리시: 파비콘 추가(콘솔 favicon.ico 404 해소) | public/favicon.svg(신규: 다크 라운드 사각+노드 3색 SVG — 캔버스 파이프라인 모티프), index.html(`<link rel="icon" type="image/svg+xml" href="/favicon.svg">`) (양쪽 앱; DFA는 막대차트 모티프 별도 아이콘, LM은 기보유) | 모든 페이지 로드마다 콘솔에 찍히던 favicon 404 제거 + 브라우저 탭 식별성. 순수 정적 자산(코드·verify 무관). 검증: build exit 0·dist/favicon.svg 포함·dist/index.html 링크 확인(3앱). ※Tailwind CDN은 4앱 공통 표준(ML 포함)으로 확인 — PostCSS 빌드 통합은 전 화면 스타일 회귀 검증이 필요한 대형 작업이라 별도 라운드 항목으로 기록만(미적용) |
| 2026-07-05 | Tailwind CDN → PostCSS 빌드 통합 (4개 앱) | package.json(+lock: tailwindcss@^3.4 · postcss@^8 · autoprefixer@^10 devDeps — ML/JMDC npm, DFA/LM pnpm), tailwind.config.js(신규: darkMode 'class'[기존 인라인 설정 동일] · content 글롭[index.html+루트/components/contexts/hooks/lib/utils ts·tsx] · safelist 5종[PropertiesPanel `text-${highlight.color}-400` 동적 조립 방어 — 현재 color 설정처는 없음]), postcss.config.js(신규), index.css(신규: @tailwind base/components/utilities), index.tsx(css import), index.html(CDN 스크립트+인라인 tailwind.config 제거 — 커스텀 `<style>` 블록[canvas-bg 등]은 유지) (4개 앱 동일 구성) | 프로덕션에서 cdn.tailwindcss.com(런타임 JIT) 사용 경고 해소 + 외부 CDN 의존 제거(오프라인/로딩 성능/결정적 CSS). 사전 조사로 동적 클래스 조립이 실질 0건임을 확인 후 전환(유일 후보 highlight.color는 미사용, LM `to-${Date.now()}`는 ID 문자열). 검증: 4앱 build exit 0 + dist CSS 생성(ML 64KB 등) + dist에 CDN 참조 0건, verify ML 27/27·JMDC 28/28·DFA 8/8·LM 6/6 PASS, tsc 4앱 0, **실브라우저 시각 스모크 4앱**: '전체 실행' 버튼 computed rgb(22,163,74)=green-600·rounded-md 6px, 다크 토글 시 html.dark→dark:bg-gray-800이 rgb(31,41,55)로 계산(ML/JMDC/DFA), LM 스타일 버튼 82개, ML은 다크 샘플 모달·캔버스(모듈 카드·미니맵·드롭다운) 스크린샷 확인. 4앱 config/css/postcss 파일 동일 |
| 2026-07-05 | ★App.tsx 분해 1단계: 통계·분석 패밀리 6분기 추출(동작 불변 리팩토링) | utils/simulationExecutors.ts(신규: executeOutlierDetector·executeHypothesisTesting·executeNormalityChecker·executeVIFChecker·executeCorrelation·executeStatistics — runSimulation 분기 본문을 **문자 그대로** 이동, 유일한 기계적 변경은 ①newOutputData 지역화+return ②동적 import 경로 ./utils/→./), App.tsx(6분기를 1줄 호출로 대체+import — ML 13,716→13,296줄[−420], JMDC 13,999→13,587줄[−412], 디스패처 체인·에러 전파[throw→catch] 구조 불변) (양쪽 앱; executors 파일 byte-identical) | 로드맵 장기 항목(runSimulation ~6,000줄 분해) 착수 — 일괄 훅화 대신 자기완결 분기만 단계 추출(사전 매핑으로 6분기가 module·getSingleInputData·addLog만 참조함을 확인). JMDC 적용 시 6분기 본문 == ML 본문 assert 후 치환(J-분기 7개 사이 배치로 3블록 분할). **동작 동일성 증명**: 결정적 iris 픽스처(6모듈 병렬, _workspace/qa_stats_family.json)를 리팩토링 전 실행해 6개 결과 모달 전체 텍스트를 기준값 저장 → 후 재실행 비교 = **6/6 문자 단위 완전 일치**(Statistics 1,148c·Correlation 869c·Outlier 779c·Normality 816c·Hypothesis 791c·VIF 358c). 비추출 분기 무회귀: 지도학습 체인 7/7 Success·Accuracy 0.7333·AUC 0.8148(기존 기록값 정확 일치). 게이트: tsc 0·build exit 0·verify ML 27/27·JMDC 28/28 PASS. 다음 단계 후보(동일 패턴): 전처리 패밀리(HandleMissing·Encode·Scaling·DataFiltering·FeatureEngineer 등)→클러스터링 정의 분기→대형(TrainModel·ScoreModel)은 의존성 주입 설계 후 최후. 상세 _workspace/5_app_decompose_step1_qa.md |
| 2026-07-05 | ★App.tsx 분해 2단계: 전처리·데이터 조작 패밀리 12분기 추출(동작 불변 리팩토링) | utils/simulationExecutors.ts(+12 실행기: SelectData·DataFiltering·Recommender·ColumnPlot·HandleMissingValues·EncodeCategorical·ScalingTransform·PythonScript·FeatureEngineer·TransitionData·Join·Concat — runSimulation 분기 본문을 스크립트로 **기계적 문자 그대로 이동**, GetSingleInputData 타입 3-인자(portType?, portName?) 확장[전처리 분기의 data_in2 호출 대응], ColumnInfo 임포트 추가), App.tsx(12분기를 1줄 호출로 대체+import — ML 13,296→12,528줄[−768], JMDC 13,579→12,811줄[−768], 디스패처 체인·에러 전파 구조 불변) (양쪽 앱; executors 파일 byte-identical) | 1단계와 동일 방법론의 확장: ①사전 스캔으로 12분기 전부 (module·getSingleInputData·addLog)만 참조하는 자기완결 확인(outputData2 기록은 module 참조 경유라 동일, Recommender NMF는 init=nndsvda·random_state=42 결정적) ②결정적 픽스처(_workspace/qa_preprocess_family.json: iris 9모듈 병렬+Join/Concat 2테이블+ratings→Recommender, 16모듈) ③지문=결과 모달 10종 innerText+전역 터미널 로그(타임스탬프·소요시간 정규화, Join 3행×3열·Concat 11행×3열 포함) ④**절차 결정성 선행 증명**(전-전 2회 독립 실행 11/11 IDENTICAL) 후 전/후 비교 = **11/11 문자 단위 완전 일치·pageErrors 0** ⑤비추출 분기 무회귀: 지도학습 체인 6/6 Success·Accuracy 0.7333·Precision 0.8571·Recall 0.6667·F1 0.7500(기존 기록값 정확 일치) ⑥JMDC는 치환 전 본문==ML 12/12 assert. 게이트: tsc 0·build exit 0·verify ML 27/27·JMDC 28/28 PASS. **검증 중 발견(미수정·다음 후보)**: Join/Concat 결과 보기 모달 무동작(shouldShowModal에 JoinOutput/ConcatOutput 누락)+Join/Concat 출력 하류 연결 불가(getSingleInputData 언랩 폴스루→null)+일부 모듈 카드 선택만으로 상태 리셋. 다음 단계: 클러스터링 정의 분기→잔여 소형→대형(TrainModel·ScoreModel, 의존성 주입 설계 필요). 상세 _workspace/6_app_decompose_step2_qa.md |
| 2026-07-05 | Join/Concat 잠복 버그 2건 수정(결과 보기 모달·하류 연결) | App.tsx(①`shouldShowModal`[-- Modals -- 블록]에 JoinOutput·ConcatOutput 2타입 추가 → 결과 보기 모달 렌더[DataPreviewModal은 이미 isJoinConcatModule 경로 보유·수정 불필요] ②`getSingleInputData` handler 분기 뒤에 JoinOutput/ConcatOutput 언랩 분기 추가[DataPreview 동형 columns+rows를 {type:DataPreview,...}로 감싸 반환] → 하류 모듈 연결 지원) (양쪽 앱) | 직전 분해 2단계 QA에서 발견한 2건: ①handleViewDetails가 두 타입을 setViewingDataForModule로 라우팅하나 렌더 조건에서 탈락해 모달 무동작 ②getSingleInputData 폴스루 null로 Join/Concat이 인앱 말단 모듈화(하류 SelectData "Input data not available"). 둘 다 **인앱 전용**(codeSnippets/export/verify 무관), 가산적. 검증: tsc 0·build exit 0·verify ML 27/27·JMDC 28/28 PASS, **실브라우저 E2E**(qa_join_concat_downstream.json, Load A/B→Join[inner on id]/Concat[vertical]→하류 SelectData 각 1): 6/6 모듈 Success[수정 전 하류 2개 실패]·Join/Concat 결과 보기 모달 열림·**Join Output 탭 3행×3열 실제 조인 결과**(id/val1/val2 = 2·20·200/3·30·300/5·50·500 = tableA⋈tableB on id={2,3,5})·pageErrors 0. getSingleInputData 언랩 블록 ML↔JMDC byte-identical, shouldShowModal 삽입은 공통 2줄 동일(JMDC는 전용 JMDCOutcomeOutput 라인 보존=의도된 차이). 상세 _workspace/7_joinconcat_fix_qa.md |
| 2026-07-05 | App.tsx 분해 3단계: 클러스터링 정의 + FeatureImportance 5분기 추출(동작 불변) | utils/simulationExecutors.ts(+5 실행기: KMeans·PrincipalComponentAnalysis·DBSCAN·HierarchicalClustering·FeatureImportance — 본문 문자 그대로 이동, ModelDefinitionOutput 임포트 추가[클러스터링 defs 캐스트]), App.tsx(5분기 1줄 호출 대체+import — ML 12,548→12,500줄[−48], JMDC 12,831→12,783줄[−48]) (양쪽 앱; executors byte-identical) | 1·2단계 동일 방법론. 경계 판정: TrainClusteringModel·ClusteringData는 connections·currentModules 참조(전자는 module.parameters 변형)로 **제외**(의존성 주입 필요, 대형과 함께 최후 순위). 도달성 메모: KMeans·PCA는 MODEL_DEFINITION_TYPES라 runSimulation에서 continue로 건너뛰는 **사문**(추출은 verbatim 안전, build/tsc 커버), DBSCAN·Hierarchical·FeatureImportance는 live→E2E 관찰. 검증: 결정적 픽스처(qa_clustering_family.json: 합성 3군집 30행→KMeans/DBSCAN/Hier 정의→각 Train/Assign+FeatureImportance, 11모듈), 지문=결과 모달 7종+정규화 터미널 로그, **절차 결정성 선행 증명**(전-전 2회 8/8 IDENTICAL) 후 전/후 **8/8 문자 단위 완전 일치·pageErrors 0**. 게이트 tsc 0·build 0·verify ML 27/27·JMDC 28/28. JMDC는 치환 전 본문==ML 5/5 assert. 상세 _workspace/8_app_decompose_step3_qa.md |
